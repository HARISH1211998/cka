apiVersion: v1
kind: ResourceQuota
metadata:
  name: ResourceQuota
  namespace: dev
spec:
  hard:
    pods: "10"
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "10"
    limits.memory: 10Gi